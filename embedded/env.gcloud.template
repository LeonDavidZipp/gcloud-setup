# =============================================================================
# GCloud Project Setup - Environment Variables
# =============================================================================
# Run: gcsetup setup
#
# The CLI will prompt for each value interactively with smart defaults.
# Pre-fill values here to skip prompts, or leave blank to be prompted.
# =============================================================================

# REQUIRED - no defaults
GCP_PROJECT_ID=
CLOUD_RUN_REGION=

# OPTIONAL - auto-detected or smart defaults
# GCP_PROJECT_NUMBER=      # fetched from GCP_PROJECT_ID
# GITHUB_ORGANIZATION=     # detected from git remote
# GITHUB_REPOSITORY=       # detected from git remote
# CLOUD_RUN_SERVICE=       # defaults to GITHUB_REPOSITORY
# SERVICE_ACCOUNT_NAME=    # defaults to "github-actions"
# ARTIFACT_REGISTRY_NAME=  # defaults to "docker"
# ARTIFACT_REGISTRY_LOCATION=  # defaults to CLOUD_RUN_REGION
